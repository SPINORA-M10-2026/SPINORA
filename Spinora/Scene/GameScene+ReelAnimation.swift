import SpriteKit

extension GameScene {
    func animateReelSpin(result: Element, duration: TimeInterval = 0.35) {
        // TODO: Person 1 owns reel animation polish based on Person 3 output.
    }
}
