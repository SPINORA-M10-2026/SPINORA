import SpriteKit

extension GameScene {
    func handleReelTouch(at point: CGPoint) {
        // TODO: Person 3 owns reel touch routing.
    }

    func handleAttackTouch(at point: CGPoint) {
        // TODO: Forward attack input to GameViewModel when integration is ready.
    }
}
